# Windows + 한글 사용자 경로(C:\Users\송기윤\...)에서 Gradle 데몬이 classpath를 담은
# 매니페스트 jar를 기본 로케일로 잘못 인코딩해서 테스트 워커가 build/classes 밑
# 클래스를 못 찾는 문제(ClassNotFoundException) 회피용.
org.gradle.jvmargs=-Dfile.encoding=UTF-8 -Dsun.jnu.encoding=UTF-8
